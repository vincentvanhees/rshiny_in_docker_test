library("HabitusGUI")
# if ("data" %in% dir()) {
#   homedir = paste0(getwd(),"/data") # cd to data directory
#   setwd(homedir)
# } else {
#   homedir = getwd()
# }
# HabitusGUI::myApp(homedir)
HabitusGUI::myApp(h)
